version: "3.9"

services:
    app:
        container_name: app
        image: ehtehasnain/my-server
        build: .
        restart: always
    app-local:
        container_name: app-run
        image: ehtehasnain/my-server
        build: .
        restart: always
        ports:
            - "20965:20965"
